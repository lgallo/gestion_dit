<h1>Listando los tipos de actividad</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tipos_requerimientos.each do |tipo_requerimiento| %>
  <tr>
    <td><%= tipo_requerimiento.nombre %></td>
    <td><%= link_to 'Show', tipo_requerimiento %></td>
    <td><%= link_to 'Edit', edit_tipo_requerimiento_path(tipo_requerimiento) %></td>
    <td><%= link_to 'Destroy', tipo_requerimiento, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tipo requerimiento', new_tipo_requerimiento_path %>

<%= form_for(@gerencium) do |f| %>
  <% if @gerencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gerencium.errors.count, "error") %> prohibited this gerencium from being saved:</h2>

      <ul>
      <% @gerencium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

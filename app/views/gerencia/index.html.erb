<h1>Listing gerencia</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @gerencia.each do |gerencium| %>
  <tr>
    <td><%= gerencium.nombre %></td>
    <td><%= link_to 'Show', gerencium %></td>
    <td><%= link_to 'Edit', edit_gerencium_path(gerencium) %></td>
    <td><%= link_to 'Destroy', gerencium, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Gerencium', new_gerencium_path %>

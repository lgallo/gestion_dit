
	<%= form_for(@aplicacion, :html => { :class => 'form-horizontal' } ) do |f| %>
  		<% if @aplicacion.errors.any? %>
    		<div id="error_explanation">
      			<h2><%= pluralize(@aplicacion.errors.count, "error") %> prohibited this aplicacion from being saved:</h2>
      			<ul>
      				<% @aplicacion.errors.full_messages.each do |msg| %>
        				<li><%= msg %></li>
      				<% end %>
      			</ul>
    		</div>
  		<% end %>

		<div class="control-group">
    		<%= f.label :nombre, :class => 'control-label' %>
    		<div class="controls"><%= f.text_field :nombre, :class => 'span3' %></div>
  		</div>
  
  		<div class="form-actions">
    		<%= f.submit 'Grabar', :class => 'btn btn-primary' %>
    		<%= link_to 'Volver', aplicaciones_path, :class => 'btn' %>
  		</div>
	<% end %>

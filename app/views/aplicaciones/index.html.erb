
	<div class="page-header">
		<h1>Listado de aplicaciones</h1>		
	</div>

	<table class="table table-bordered table-striped table-condensed">
		<thead>
			<tr>
    			<th>Nombre</th>
    			<th></th>
    			<th></th>
    			<th></th>
  			</tr>
		</thead>
		<tbody>
			<% @aplicaciones.each do |aplicacion| %>
  				<tr>
				    <td><%= aplicacion.nombre %></td>
				    <td><%= link_to 'Show', aplicacion %></td>
				    <td><%= link_to 'Edit', edit_aplicacion_path(aplicacion) %></td>
				    <td><%= link_to 'Destroy', aplicacion, confirm: 'Are you sure?', method: :delete %></td>
  				</tr>
			<% end %>
		</tbody>
	</table>

	<div class="form-actions">
		<%= link_to 'Nueva aplicación', new_aplicacion_path, :class => 'btn btn-primary' %>
	</div>
